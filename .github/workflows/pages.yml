name: build pages

on:
  push:
    branches:
      - main
  workflow_dispatch:
    # I need to add dependencies
  # and also actions...


# permissions?

# concurrency?

jobs:
  build:
    runs-on: ubuntu-latest

  deploy:
    environment:
      name: github-pages

    runs-on: ubuntu-latest
    needs: build

    # steps?